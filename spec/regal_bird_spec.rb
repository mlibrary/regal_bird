require "spec_helper"

describe RegalBird do
  it "has a version number" do
    expect(RegalBird::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
